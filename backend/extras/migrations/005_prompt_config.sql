-- 005: настройки генерации на пользователя
--
-- Страница /prompts меняла конфиг только в стейте React: обновление вкладки
-- возвращало значения по умолчанию. В api.js при этом уже были методы
-- promptConfig.get/set, но соответствующего маршрута на бэкенде не было —
-- их никто не вызывал. Храним конфиг рядом с пользователем.

ALTER TABLE users
  ADD COLUMN IF NOT EXISTS prompt_config JSONB;
